# API URL — points to your backend server
# For local dev on same machine:
VITE_API_URL=http://192.168.1.10:5000

# For phone access on same Wi-Fi, this should be your PC's local IP
# Change this if your IP changes (run `ipconfig` to check)
